name: Run updates feedback patch

on:
  push:
    branches:
      - feature/telegram-miniapp-webgl
    paths:
      - .github/scripts/patch-updates-feedback.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/telegram-miniapp-webgl
      - name: Apply Mini App updates and feedback wiring
        run: python .github/scripts/patch-updates-feedback.py
      - name: Commit wiring
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add miniapp/server.js
          git diff --cached --check
          git commit -m "feat: wire updates and feedback Mini App APIs"
          git push origin HEAD:feature/telegram-miniapp-webgl
